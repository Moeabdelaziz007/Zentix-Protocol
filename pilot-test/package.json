{
  "name": "amrikyy-zentix-pilot-test",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently \"npm run protocol:dev\" \"npm run desktop:dev\"",
    "protocol:dev": "cd protocol && npm run dev",
    "desktop:dev": "cd desktop && npm run dev",
    "setup": "cd protocol && npm install && cd ../desktop && npm install && cd .. && echo '✅ Setup complete! Run `npm run dev` to start the application'"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "dotenv": "^16.6.1"
  }
}
